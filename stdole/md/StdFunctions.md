# StdFunctions

**Type:** Module  
**Library:** OLE Automation  

Functions for Standard OLE Objects

## Functions (2)

- `LoadPicture([filename As Variant], [widthDesired As Long], [heightDesired As Long], [flags As LoadPictureConstants], retval As IPictureDisp)`  
  Loads a picture from a file
- `SavePicture(Picture As IPictureDisp, filename As String)`  
  Saves a picture to a file
