{
  "name": "WdMailMergeDefaultRecord",
  "kind": "Enumeration",
  "library": "Microsoft Word 16.0 Object Library",
  "description": "Specifies the default record to use in a mail merge process.",
  "constants": [
    {
      "name": "wdDefaultFirstRecord",
      "value": 1,
      "description": "Use the first record in the result set as the default record."
    },
    {
      "name": "wdDefaultLastRecord",
      "value": -16,
      "description": "Use the last record in the result set as the default record."
    }
  ]
}
